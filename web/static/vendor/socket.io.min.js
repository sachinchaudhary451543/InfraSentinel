/*
  Placeholder for local socket.io-client bundle.

  DO NOT commit the upstream socket.io-client code if you cannot license it.
  To enable the local fallback, download a matching socket.io-client v4.x minified
  build and place it here. Example (locally):

  curl -L https://cdn.socket.io/4.6.1/socket.io.min.js -o socket.io.min.js

  Then restart/redeploy the app so static files are served.
*/

// Minimal no-op shim to prevent immediate JS errors if the real client isn't present.
// NOTE: Replace this file with the official socket.io-client build for production.
if (typeof io === 'undefined') {
  window.io = function() {
    console.warn('socket.io client shim in use — replace with real socket.io-client at web/static/vendor/socket.io.min.js');
    return { on: function(){}, emit: function(){}, close: function(){}, connect: function(){} };
  };
}
